## Select Sheet

- Margin/padding issue
  - when adding safe area padding to the bottom sheet, the scroll container doesn't allow the items to scroll to the bottom. e.g. the scroll container doesn't shrink, so part of the container is off screen
